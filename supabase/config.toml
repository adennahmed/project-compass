# NOTE: project_id should be replaced when linking to a fresh Supabase
# project. Use `supabase link --project-ref <new-ref>` to update.
project_id = "mmuicazqthylbhcnolax"

[functions.delete-account]
verify_jwt = true
